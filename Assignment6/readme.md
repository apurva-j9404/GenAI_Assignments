# Text-to-Image Generation using Pretrained Model

This assignment implements a text-to-image generation pipeline using a pretrained diffusion model. The system takes a natural language prompt as input and generates corresponding images using a deep learning model similar to DALL-E. The implementation uses Python, PyTorch, and HuggingFace Diffusers.
