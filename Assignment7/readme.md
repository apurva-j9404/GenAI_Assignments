# Image Captioning using CLIP

This assignment implements a basic multimodal system for image captioning using the CLIP model. The system compares an image with multiple candidate captions and selects the most relevant caption based on similarity scores. The implementation uses Python, PyTorch, and HuggingFace Transformers.
